{
  "name": "leadermath-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "type": "module",
  "scripts": {
    "build": "tsc -p .",
    "deploy": "firebase deploy --only functions:submitTestResult",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
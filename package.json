{
  "name": "leader-platform",
  "private": true,
  "version": "1.0.1",
  "description": "Static multi-page platform + Netlify Functions (Firestore) with build script for Netlify",
  "scripts": {
    "build": "echo \"static site - no build\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "firebase-admin": "^12.5.0",
    "jsonwebtoken": "^9.0.2"
  },
  "engines": {
    "node": ">=18"
  }
}
{
  "name": "leadermath-api-site",
  "version": "1.0.0",
  "private": true,
  "description": "LeaderMath.uz — Full API-driven (Netlify Functions + MongoDB) + static frontend",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo \"static site\""
  },
  "dependencies": {
    "mongodb": "^6.6.2"
  }
}
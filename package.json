{
  "name": "leadermath-firebase-api-only",
  "version": "1.0.0",
  "private": true,
  "description": "LeaderMath full platform — Firebase Admin via Netlify Functions (no client SDK) + premium mobile UI",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo static"
  },
  "dependencies": {
    "firebase-admin": "^12.3.1"
  }
}
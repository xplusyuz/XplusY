// js/common.js — safe stubs
export function attachAuthUI({ requireSignIn = false } = {}){
  // stub: real auth UI can be integrated here
  // no-op to avoid errors
}
export function initUX(){
  // stub: place for global UI init (toasts, etc.)
}

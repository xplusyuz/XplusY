LeaderMath.bot-v3 (fixed version)
----------------------------------
✅ Firebase.js is now a clean ES module (no <script> tags).
✅ Auth & admin gating will work correctly on Netlify.
✅ Replace the old firebase.js file with this one in /public/.
Then redeploy your site.
